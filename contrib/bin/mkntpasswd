#!/usr/bin/perl
use strict;
use 5.010;

use Crypt::SmbHash;

=pod
Render smb hash of the password given as first parameter
=cut

my $pwd;

if(@ARGV>0) {
    $pwd = shift @ARGV;
} else {
	say "Usage: $0 <password>";
	exit 1;
}

# Render hash
my ( $lm, $nt ) = ntlmgen $pwd;
say "$lm:$nt";

exit 0;
